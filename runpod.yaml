version: "1"

runtime:
  type: python
  version: "3.10"
  entrypoint: handler.py
  requirementsPath: requirements.txt   # optional; detected automatically

resources:
  gpu: 0         # CPU runtime
  maxWorkers: 3
  activeWorkers: 0
  idleTimeout: 5
  executionTimeout: 600
